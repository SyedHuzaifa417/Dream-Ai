import React from "react";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Settings",
  description: "AI-powered image and video generation",
};

const page = () => {
  return <div className="text-white text-center">to be designed yet</div>;
};

export default page;
